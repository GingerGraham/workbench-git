name: Release Finalize

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  finalize:
    # module-release-finalize.yml gates internally on merged + release/v*.
    # TODO: bump to @main once GingerGraham/workbench-core#33 (D40) merges.
    # Pinned to that PR's branch HEAD sha (reusable-workflow `uses:` refs
    # can't contain a "/", so the branch name itself can't be used here).
    uses: GingerGraham/workbench-core/.github/workflows/module-release-finalize.yml@5c25cdffdede67c318a7aa997a5da691dc99d35c
    secrets: inherit
