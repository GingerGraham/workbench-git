name: PR Check

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  commit-format:
    # Pinned to a core release tag (workbench-core D78); core_ref defaults to
    # core's latest release.
    permissions:
      contents: read
    uses: GingerGraham/workbench-core/.github/workflows/module-pr-check.yml@v2.16.0
