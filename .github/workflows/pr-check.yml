name: PR Check

on:
  pull_request:
    branches: [main]

jobs:
  commit-format:
    # TODO: bump to @main and drop core_ref once GingerGraham/workbench-core#33 (D40) merges.
    uses: GingerGraham/workbench-core/.github/workflows/module-pr-check.yml@claude/workbench-extension-ci-ska0he
    with:
      core_ref: claude/workbench-extension-ci-ska0he
